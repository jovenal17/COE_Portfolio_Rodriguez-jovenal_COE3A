Project 1
Simple calculator c++

#include <iostream>
using namespace std;

int main() {
    int num1, num2;
    char op;

    cout << "=============================\n";
    cout << "     SIMPLE CALCULATOR       \n";
    cout << "=============================\n";

    cout << "Enter first number: ";
    cin >> num1;

    cout << "Enter operator (+, -, *, /): ";
    cin >> op;

    cout << "Enter second number: ";
    cin >> num2;

    cout << "-----------------------------\n";
    cout << "RESULT: ";

    switch(op) {
        case '+':
            cout << num1 + num2 << endl;
            break;
        case '-':
            cout << num1 - num2 << endl;
            break;
        case '*':
            cout << num1 * num2 << endl;
            break;
        case '/':
            if (num2 != 0)
                cout << num1 / num2 << endl;
            else
                cout << "Error! Cannot divide by zero." << endl;
            break;
        default:
            cout << "Invalid operator!" << endl;
    }

    cout << "=============================\n";
    cout << "      PROGRAM ENDED          \n";
    cout << "=============================\n";

    return 0;
}


Project 2

print("=============================")
print("   NUMBER CONVERTER TOOL     ")
print("=============================")

# Get input from user
decimal_num = int(input("Enter a Decimal Number: "))

# Convert
binary_num = bin(decimal_num)       # gives like '0b101'
hex_num = hex(decimal_num)          # gives like '0xA'

# Show result
print("\n----- RESULT -----")
print(f"Decimal  : {decimal_num}")
print(f"Binary   : {binary_num[2:]}")   # remove '0b'
print(f"Hexadecimal: {hex_num[2:].upper()}") # remove '0x' & make uppercase

print("\n=============================")
print("       PROGRAM ENDED         ")
print("=============================")

